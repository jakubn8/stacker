rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Server-side only - all access via Admin SDK
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
